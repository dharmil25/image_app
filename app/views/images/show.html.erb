<% provide(:title, @image.tagline) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @image.album.user %>
        <%= @image.album.user.name %>
      </h1>
      <h5>
      <%= @image.tagline %>
      </h5>
    </section>
  </aside>
</div>